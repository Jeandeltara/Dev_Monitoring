name: Media Analysis Workflow

on:
  schedule:
    # Запуск щодня о 09:00 за UTC
    - cron: '0 9 * * *'
  # Можливість запустити вручну з вкладки Actions
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Устанавливаем библиотеку для работы с Groq
          python -m pip install requests beautifulsoup4 groq

      - name: Run media analysis script
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python media_analysis.py

      - name: Commit and push changes
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          # Додаємо результати аналізу та промпт, якщо вони з'явилися
          git add *_media_analysis.txt prompt_w.txt || echo "No files to add"
          git commit -m "Update media analysis report [skip ci]" || echo "No changes to commit"
          git push
